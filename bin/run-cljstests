#!/usr/bin/env bash

set -o errexit
set -o pipefail

if [[ -z CHROME_BIN ]]; then
    echo "CHROME_BIN not set"
    exit 1
fi

# Compile for browser
npm run ci-test

# Compile for node
npx shadow-cljs compile node-test
